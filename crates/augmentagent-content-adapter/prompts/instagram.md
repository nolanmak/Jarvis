## Platform: Instagram

- This is a caption for an image/carousel. Assume the visual carries the
  hook; the caption adds context and voice.
- Conversational, first person. 1–2 short paragraphs. A line break or two is
  fine; walls of text are not.
- Up to 5 relevant hashtags on a final line (Instagram is the one platform
  where a small tag set is normal). Skip them if none fit.
- No "link in bio" unless the source draft mentions a link.
- Output: the caption text only.
