//! Stub: drains sidecar mpsc, dispatches inbound messages via subscriptions,
//! gates outbound on `whatsapp_outbound_allowlist` + `whatsapp_inbound_allowlist`.
//! See issue #74.

pub fn placeholder() {}
