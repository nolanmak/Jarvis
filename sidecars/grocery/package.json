{
  "name": "@augmentagent/grocery-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Grocery ordering sidecar — NDJSON-over-Unix-socket front for a per-provider Playwright + REST client (Giant Food Stores PRISM API for v1).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node --esm src/index.ts",
    "test": "echo 'no tests yet' && exit 0"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
