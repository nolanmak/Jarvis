diff --git a/open-sse/executors/default.js b/open-sse/executors/default.js
index 00f3e3d..4515a59 100644
--- a/open-sse/executors/default.js
+++ b/open-sse/executors/default.js
@@ -149,6 +149,14 @@ export class DefaultExecutor extends BaseExecutor {
   buildHeaders(credentials, stream = true, url, model) {
     const rt = credentials?.runtimeTransport;
     const headers = { "Content-Type": "application/json", ...(rt ? rt.headers : this.config.headers) };
+    // Preserve the caller's logical-turn key for compatible gateways. A retry
+    // without this key can submit the same paid Runpod job a second time.
+    if (this.provider?.startsWith?.("openai-compatible-")) {
+      const key = credentials?.rawHeaders?.["idempotency-key"];
+      if (typeof key === "string" && key.length > 0 && key.length <= 128) {
+        headers["Idempotency-Key"] = key;
+      }
+    }
     const desc = rt?.auth || AUTH_DESCRIPTORS[this.provider] || this.resolveAuthDescriptor();
     // Hooks run BEFORE auth so dynamic overlays can't clobber the token.
     for (const hook of desc.hooks || []) HEADER_HOOKS[hook]?.(headers, credentials);
diff --git a/open-sse/services/combo.js b/open-sse/services/combo.js
index e3b9a8e..11ed4ea 100644
--- a/open-sse/services/combo.js
+++ b/open-sse/services/combo.js
@@ -102,7 +102,7 @@ function trailingUserItems(arr) {
 // Detect which capabilities a request needs. Modalities (vision/pdf) are scanned
 // only on the current user turn; "search" is request-wide (lives in tools).
 // Returns a Set of: "vision" | "pdf" | "search".
-export function detectRequiredCapabilities(body) {
+export function detectRequiredCapabilities(body, { includeHistory = false } = {}) {
   const required = new Set();
   if (!body || typeof body !== "object") return required;
 
@@ -172,11 +172,13 @@ export function detectRequiredCapabilities(body) {
     }
   };
 
-  // Modalities: current user turn only (trailing user run across each known shape).
-  for (const m of trailingUserItems(body.messages)) scanMessage(m);              // openai / claude / hermes / ollama
-  for (const it of trailingUserItems(body.input)) scanContent(it.content);       // responses
+  // Capacity-adapter ordering uses the current turn by default. An explicitly
+  // pinned compatible node can request all turns to prevent history media loss.
+  const selected = (items) => includeHistory ? (Array.isArray(items) ? items : []) : trailingUserItems(items);
+  for (const m of selected(body.messages)) scanMessage(m);              // openai / claude / hermes / ollama
+  for (const it of selected(body.input)) scanContent(it.content);       // responses
   const contents = body.contents || body.request?.contents;                      // gemini / antigravity
-  for (const c of trailingUserItems(contents)) scanContent(c.parts);
+  for (const c of selected(contents)) scanContent(c.parts);
 
   // search: temporarily disabled in auto-switch (feature not wired yet).
 
diff --git a/src/sse/handlers/chat.js b/src/sse/handlers/chat.js
index 7aa530d..5b70156 100644
--- a/src/sse/handlers/chat.js
+++ b/src/sse/handlers/chat.js
@@ -19,6 +19,7 @@ import { handleComboChat, handleFusionChat, detectRequiredCapabilities } from "o
 import { augmentModelsWithCapacityAdapter, withCapacityAdapterStripping, getActiveAdapterStrategy } from "open-sse/services/capacityAdapter.js";
 import { handleBypassRequest } from "open-sse/utils/bypassHandler.js";
 import { HTTP_STATUS } from "open-sse/config/runtimeConfig.js";
+import { getCapabilitiesForModel } from "open-sse/providers/capabilities.js";
 import { detectFormatByEndpoint } from "open-sse/translator/formats.js";
 import * as log from "../utils/logger.js";
 import { updateProviderCredentials, checkAndRefreshToken } from "../services/tokenRefresh.js";
@@ -138,6 +139,13 @@ export async function handleChat(request, clientRawRequest = null) {
     });
   }
 
+  // An explicit compatible node is a pinned backend. A capacity adapter must
+  // not silently send its prompt or attachment to a different provider.
+  const explicitModel = await getModelInfo(modelStr);
+  if (explicitModel.provider?.startsWith("openai-compatible-")) {
+    return handleSingleModelChat(body, modelStr, clientRawRequest, request, apiKey);
+  }
+
   // Single model request — may still switch to a capacity-adapter model if the
   // target lacks a capability the request needs (e.g. no vision, request has an image).
   const soloAugmented = augmentModelsWithCapacityAdapter([modelStr], requiredCapabilities, settings);
@@ -220,6 +228,18 @@ async function handleSingleModelChat(body, modelStr, clientRawRequest = null, re
 
   const { provider, model } = modelInfo;
 
+  // An explicit compatible-model selection must never answer after silently
+  // dropping a current-turn attachment it cannot read.
+  if (provider.startsWith("openai-compatible-")) {
+    const required = detectRequiredCapabilities(body, { includeHistory: true });
+    const capabilities = getCapabilitiesForModel(provider, model);
+    const unsupported = ["vision", "pdf", "audioInput", "videoInput"]
+      .filter((capability) => required.has(capability) && capabilities[capability] !== true);
+    if (unsupported.length) {
+      return errorResponse(422, `Model cannot read requested input: ${unsupported.join(", ")}`);
+    }
+  }
+
   // Routing shown in the unified "▶" line (client model → provider/model)
 
   // Extract userAgent from request
@@ -309,6 +329,12 @@ async function handleSingleModelChat(body, modelStr, clientRawRequest = null, re
 
     if (result.success) return result.response;
 
+    // A compatible gateway's 409 requires caller reconciliation. Account
+    // failover would hide it as 503 and could submit the same work elsewhere.
+    if (provider.startsWith("openai-compatible-") && result.status === 409) {
+      return result.response;
+    }
+
     // Antigravity 409/429: refresh live quota to get exact resetAt before locking
     let quotaResetMs = null;
     let resetsAtMs = result.resetsAtMs;
